/* // lib/db/schema.ts
import prisma from './index'

export async function initTables() {
  // Prisma 会自动根据 schema 创建表结构
  // 这个函数现在主要用于其他初始化逻辑
  console.log('Database tables initialized via Prisma schema')
} */